# Experiments

Sandbox/prototype work that is not the default production training path.

Current content:
- `diffusion_prototype/`: prototype latent diffusion workflow.

Use `models/train/core/train.py` + `slurm/` launchers for the mainline workflow.
